# Toy demo dependencies (CPU-only, lightweight). Tested on Python 3.13.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7      # GIF export (uses the bundled Pillow writer, no ffmpeg)
plotly>=5.20         # interactive figures + standalone HTML export
dash>=2.16           # live interactive app (interactive_viz.py)
torch>=2.0            # rollout experiment only (train_rollout.py); the static
                      # demo stays hand-written numpy with no autograd framework
